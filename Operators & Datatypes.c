#include <stdio.h>

int main() {
    int a = 10, b = 5;
    int sum = a + b;      // arithmetic operator
    int isGreater = a > b; // relational operator (1 if true, 0 if false)
    int c = a * b;        // multiplication

    printf("Sum: %d\n", sum);
    printf("Is a greater than b? %d\n", isGreater);

    return 0;
}
